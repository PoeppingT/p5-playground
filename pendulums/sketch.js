function setup() {
  createCanvas(windowWidth, windowHeight);
  colorMode(HSB);
  curves.push(randomCurve())
  text('pendulums and double pendulums animation and control', 10, 10);
}

function draw() {
  
}